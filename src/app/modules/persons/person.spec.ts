/*import {
    beforeEach,
    describe, xdescribe,
    expect, it, xit,
    async, inject
} from '@angular/core/testing';

import {Person} from './person';

describe('Persons', () =>{
   it('should create instance of', () => {
       expect(new Person()).toBeTruthy();
   });
   it('should get value into constructor', () =>{
       var person = new Person(
           {
               accountId: 120,
               additionalInfo: 'additionalInfo',
               additionalPhone: 'additionalPhone',
               address: 'address',
               birthDay: 'birthDay',
               birthMonth: 'birthMonth',
               birthYear: 'birthYear',
               cardAvailability: true,
               cardNumber: 'cardNumber',
               contactPerson: 'contactPerson',
               description: 'description',
               divisionNumber: 'divisionNumber',
               email: 'email',
               gender: 'gender',
               howDidYouKnowAnother: 'howDidYouKnowAnother',
               howDidYouKnowId: 'howDidYouKnowId',
               name: 'name',
               phone: 'phone',
               tabNumber: 315
           }
       );
   });

   expect(person.accountId).toEqual(120);
   expect(person.additionalInfo).toEqual('additionalInfo');
   expect(person.tabNumber).toEqual(315);
});*/
